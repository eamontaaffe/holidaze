language: node_js
node_js:
  - "7"
branches:
  only:
  - master
cache:
  yarn: true
  directories:
    - node_modules
    - .cache
    - public
deploy:
  provider: script
  script: 'ls $TRAVIS_BUILD_DIR && yarn deploy:ci'
  skip_cleanup: true
  on:
    branch: master
